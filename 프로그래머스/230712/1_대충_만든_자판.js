function solution(keymap, targets) {
  var answer = [];

  targets.forEach((item, index) => {
    const targetArr = item.split("");
    targetArr.forEach((i) => {
        if()
    })
  });

  return answer;
}

solution(["ABACD", "BCEFD"], ["ABCD", "AABB"]);
